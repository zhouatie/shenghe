## 1. 数据模型

- [x] 1.1 在 `lib/types.ts` 增加板块榜单、板块曲线、板块龙头、刷新结果和市场评分建议类型
- [x] 1.2 在 SQLite 初始化逻辑中新增板块榜单快照、板块详情和刷新状态存储
- [x] 1.3 增加读取最近板块数据、写入刷新结果和读取板块详情的数据库函数
- [x] 1.4 确保旧数据库启动时无需迁移脚本即可创建新增表，且不影响候选池、持仓和复盘读取

## 2. 板块接口服务

- [x] 2.1 新增 `lib/plate-rotation.ts`，用 TypeScript `fetch` 封装板块轮动接口请求
- [x] 2.2 实现开盘啦和同花顺今日榜解析，保留 `source`、`valueType`、排名、代码、名称和数值
- [x] 2.3 实现 Top5 排名曲线解析，并识别未上榜数据
- [x] 2.4 实现板块龙头矩阵和板块强度时序解析，支持当日无领涨为空列表
- [x] 2.5 增加请求超时、有限重试、接口空数据和解析失败的错误结果

## 3. 市场评分建议

- [x] 3.1 新增板块数据到市场 60 分建议值的映射函数
- [x] 3.2 为 `rs`、`themeScore`、`flow`、`structure` 和 `catalyst` 生成建议值和证据说明
- [x] 3.3 确保建议逻辑不写入或推导 `growth`、`position`、`valuation`、`cashflow` 和 `governance`
- [x] 3.4 支持用户采纳建议后只更新市场评分字段

## 4. Server Actions 与应用状态

- [x] 4.1 在 `app/actions.ts` 增加刷新板块数据的服务端动作
- [x] 4.2 扩展 `getAppState()` 返回最近板块榜单、刷新状态和主线雷达所需板块摘要
- [x] 4.3 扩展导出接口返回板块数据摘要或包含板块数据的聚合状态
- [x] 4.4 保持现有 Yahoo Finance `refreshQuotes` 行为不变

## 5. 用户界面

- [x] 5.1 在数据台增加“刷新板块”入口，并展示成功、空数据和失败状态
- [x] 5.2 在主线雷达展示开盘啦强度榜、同花顺涨幅榜和来源标识
- [x] 5.3 在主题卡或详情区域展示板块排名曲线、转折信号和未上榜状态
- [x] 5.4 在候选标的表单或逻辑卡中展示市场评分建议和采纳入口
- [x] 5.5 在板块详情中展示龙头候选草稿，并支持确认入池或放弃
- [x] 5.6 更新页面风险提示，说明板块数据来源和市场评分建议边界

## 6. 验证

- [x] 6.1 为板块榜单、曲线、龙头矩阵和强度时序解析补充最小测试或可复用夹具
- [x] 6.2 验证双源数据不会跨量纲混算
- [x] 6.3 验证采纳市场评分建议不会修改基本面 40 分
- [x] 6.4 验证无板块数据或接口失败时，原候选池、主线雷达、持仓体检和复盘日记仍可使用
